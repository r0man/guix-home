#!/bin/sh
#
# ~/.xsession
#
# https://wiki.debian.org/Xsession
#

# Load Guix Home.
if [ -r ~/.profile ]; then . ~/.profile; fi

# Load ~/.xprofile
[[ -f ~/.xprofile ]] && . ~/.xprofile

# Start StumpWM
dbus-launch sbcl --load $HOME/.config/stumpwm/start
